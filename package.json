{
  "name": "jira-sdlc-validator-mock",
  "version": "1.0.0",
  "private": true,
  "description": "Simple Node.js mock Jira issue browser",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "deploy:cloudflare": "npx wrangler pages deploy public",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
